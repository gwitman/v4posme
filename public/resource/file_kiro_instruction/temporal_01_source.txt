tengo una baes de datos  la estructura de la base de datos la encuentras en el archivo 
#posme_db_structure.sql, cremae una script para una base de datos mysql que reciba como 
variable el scrito un @workflowStageName es el nombre del estado por ejemplo: 
'ACTIVO', 'ELIMINADO', 'PROSPECTOS' y adicional el @workflowID que 
es un entero del catalogo de estado,  
el otro parametro es el @flavorID int es un entero representativo 
el objetivo es que hagas un con los varaible que te mencione anteriormente, 
el insert en la tabla tbl_workkflow_stage con el flavor indicado, una ves echo el inesrt en la tabla, 
agreges a la tabla   tb_workflow_stage_relation que ese estado nuevo, puede pasar por 
todos los estados que ya exitente, y que todos los estados existente, pueden pasar
por el estado nuevo, entiendase que cuando digo todo los estaod son los estados que
sean del mismo workflowID ,  

caos a tomar en cuenta: 
en caso que ya existe esa relacion, de orige a destino 
no ingresarla en caso que no existe esa relacion, ingresarla .

en caso que ya exista el estado workflowStageName con el mismo flavor, no ingrearlo 
pero tomar el id para hacer el insert en la tabla tb_workflow_stage_relation.  

parte 3: 
una ves que el estado se ingres en la relacion,  asociar,
el nuevo workflowStageID a loas autoriaciones para hacer eso, es necesario 
hacer un insert en la tabla : tb_component_autorization_detail
donde el companyID siempre es 2, el  componentAutorizationID siempre es 4,
el componentID, el workflowSID, workflowStageID los scas del reigstro que acabas 
de insertar enla tabla workflowStage ; 

la slida del script resultante dejala caer en el archivo  #temporal_02_target.txt 
para luego yo probarla y revisarlo 